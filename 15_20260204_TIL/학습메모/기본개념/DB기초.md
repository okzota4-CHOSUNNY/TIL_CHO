## PK와 FK
1. PK (Primary Key, 기본키) - "신분증"
- 테이블에서 각 행(Row)을 유일하게 식별할 수 있는 핵심 컬럼.
- 중복 불가 (Unique), 비어있음 불가 (Not Null).

2. FK (Foreign Key, 외래키) - "연결고리"
- 다른 테이블의 PK를 참조하는 컬럼. 테이블 간의 관계를 형성.
- 데이터 무결성: 참조하는 테이블에 실제 존재하는 값만 가질 수 있도록 제한.

3. 요약: PK는 각 행을 고유하게 식별, FK는 다른 테이블과의 관계를 형성.

# Git 최초 설정
git config -- system : 시스템 설정(모든 사용자 및 저장소에 적용)
git config -- global : 전역 설정 (모든 저장소에 적용)
git config -- local  : 로컬 설정 (현재 저장소에만 적용)

git config --global user.name "사용자 이름"


git config --global user.name "사용자 이메일"

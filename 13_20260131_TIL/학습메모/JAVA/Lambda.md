```java
람다식(람다 표현식, Lambda Expression)이란?

1. 람다식란?
 - "이름없는 함수" 익명함수
2. 탄생배경
 - 코드 간결화, 함수형 프로그래밍 지원, 병렬 처리 용이

3. 람다식 4대 핵심문법 양식(2가지만 짚고가자)
 1. 변환기(A를 가져오려면 B로 바꿔라)
 - 실무에서 가장 많이 하는일은 '데이터 가공'. DB에서 가져온 데이터를(Entity)를 화면에 보여줄 데이터(DTO)로 바꿀때 람다가 많이 쓰임
 2. 코드예시
// 게시글(Board) 객체에서 '제목'만 뽑아서 리스트로 반환
boardList.stream(여기 괄호에 뭐가 없다?!)
    .map(board -> board.getTitle());
    // 해석: boardList에 있는 각각의 board 객체에서 getTitle() 메서드를 호출하여 제목을 추출
<nav class="navbar bg-dark navbar-expand-lg bg-body-tertiary" data-bs-theme="dark">
해석: nav(내비게이션 바, 탐색 막대): 웹사이트의 주요 메뉴나 링크를 포함하는 상단 바
navbar bg-dark navbar-expand-lg bg-body-tertiary: 부트스트랩 클래스
data-bs-theme="dark": 다크 테마 적용
<nav class="navbar navbar-expand-lg" style="background-color: #fffe100 !important;">
해석: nav(내비게이션 바, 탐색 막대): 웹사이트의 주요 메뉴나 링크를 포함하는 상단 바
navbar navbar-expand-lg: 부트스트랩 클래스
style="background-color: #ffe100 !important;": 배경색을 노란색(#ffe100)으로 강제 지정


``` java
	public int delete(String id) {
    // public으로 접근 가능한 반환타입이 int인 delete 메서드를 정의한다(id 매개변수 값은 String 타입)
		int result = 0;
        // int 타입의 result 변수를 0으로 초기화한다.
        user result = null;
        // User 타입의 result 변수를 null(값이 없음)로 초기화한다.
        string result = "";
        // String 타입을 빈값으로 초기화한다.
		String sql = "DELETE FROM `user` WHERE id = ? ";
		try {
			psmt = con.prepareStatement(sql);
			// DB와 연결
			psmt.setString(1, id);
			
			result = psmt.executeUpdate();
			
		} catch (Exception e) {
            e.printStackTrace();
			// 예외 처리: try에서 실행되고 발생한 오류가 발생하면 catch를 실행한다.
		} 
		return result;
        // 결과값 반환이란: 모든 과정을 마친 후 최종 결과값을 호출한 곳으로 돌려보내는 것


       	public List<Product> list() {
		// TODO: 전체 상품 목록 조회
		ex) public void(void: 반환x)
		 
	
	}
```